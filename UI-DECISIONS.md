# 已确认的工作台设计

2026-09-05，用户逐项选择并授权实施。

- 左侧固定分类；项目概览、角色与机甲、场景参考、镜头图、分镜工作稿、视频片段、视频截帧、音频、视频分析与来源、Seedance控制、制作记录。
- 浅灰白底、深色文字、少量蓝色强调；系统无衬线正文14–15px。
- 细灰边框、适度圆角，仅详情浮层与放大预览使用阴影。
- 默认查看，点击编辑进入编辑模式；顶部固定保存与取消；未保存离开提醒。
- 分镜为左镜头列表、中内容、右参考素材；窄屏参考区移至内容下方。
- 素材卡片中等大小，桌面每行3–4张，可切换大小；原图完整显示、浅灰补边。
- 点击素材打开右侧详情，保留列表；支持放大。
- 拖入当前分类批量上传，随后编辑来源、用途与分镜关联；音频/视频保持实际媒体类型。
- 右侧素材选择器支持分类筛选、搜索、多选关联。
- 多选素材、全选当前筛选结果、原文件ZIP下载、重名序号、进度和失败重试；可下载整镜参考素材。

## 实施与检查

- 独立应用，不修改 PixelVault 页面；保留现有项目数据与停用标记。
- 浏览器检查默认查看、编辑保存、分类素材选择器、多选关联和退出编辑。
- 浏览器实际下载7份语音ZIP，CRC与逐文件SHA-256检查通过。
- TypeScript与生产构建检查通过。尚未部署云端。
- 当前单文件上传50MB；ZIP在浏览器内存中打包，建议每批不超过512MB。视频截帧分类支持上传，自动截帧工具不在本次变更内。

## 视频预览与主图编辑补充

- 视频素材卡直接显示视频首帧和原生播放、进度、音量及全屏控件；点击名称仍可打开详情。
- 分镜编辑模式增加选择已有图片、上传替换主图；通过 heroAssetId 保存所选主图，保留旧素材。
- 浏览器已确认卡片真实画面、播放控件与主图选择列表可显示。未改动已有分镜数据。
- 中文字体方案待用户选择，采用随站点托管字体文件的方式。

- 2026-09-06 用户确认霞鹜新晰黑屏幕阅读版。使用官方26.08.21原始TTF，许可随站点保留；本地托管，无第三方字体请求。辅助标签12px、常用控件13px、正文14px，调深辅助文本。

## 音频与完整素材补齐 · 2026-09-06
- 音频卡片改为直接播放，显示由真实采样数据计算的波形；读取失败仍保留播放器。
- 增量导入235张原片截帧/复核总览、25张遗漏参考图片，内容哈希去重；保留采样时间、来源与已知分镜关联。再次扫描无遗漏。
- 导入通过项目API的revision校验保存；现有内容不重建。类型检查与生产构建通过。

## 项目概览接续信息 · 2026-09-06
用户要求直观展示当前进度与画风基准。首页默认项目概览；stage/completed/next可编辑；styleReferences可选择已有图片并注明使用范围。第22镜为整体视觉、达妮娅为角色画风校准。数据为云端项目字段，助手指南说明读取顺序。

## 详情面板关闭 · 2026-09-06
点击素材详情外部关闭面板，面板内操作保留；放大预览、素材选择器及保存期间不触发外部关闭。关闭仅收起面板，编辑草稿仍由顶部保存。浏览器验证内外点击通过，类型检查和构建通过。
